# TODO Realizar los pasos para crear el servicio
# Run cd /etc/systemd/system/
# Run sudo nano flask.service

[Unit]
Description = flask python command to do useful stuff

[Service]
ExecStart = /home/pi/flask.sh

[Install]
WantedBy = multi-user.target

# TODO realizar el siguiente paso para habilitar el servicio
# Run sudo systemctl start flask
# Run sudo systemctl enable flask.service
# Run journalctl -u flask.service (q to exit)